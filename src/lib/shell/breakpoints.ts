export const SHELL_NARROW_MAX_PX = 720;
export const SHELL_MEDIUM_MAX_PX = 1180;

export const SHELL_NARROW_QUERY = `(max-width: ${SHELL_NARROW_MAX_PX}px)`;
export const SHELL_MEDIUM_QUERY = `(max-width: ${SHELL_MEDIUM_MAX_PX}px)`;
export const SHELL_WIDE_QUERY = `(min-width: ${SHELL_MEDIUM_MAX_PX + 1}px)`;
